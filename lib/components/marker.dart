import 'package:flutter/material.dart';

class marker_map extends StatelessWidget {
  const marker_map({super.key});

  @override
  Widget build(BuildContext context) {
    return Image.asset("assets/Marker.png");
  }
}